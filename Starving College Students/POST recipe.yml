info:
  name: POST recipe
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8080/recipes
  body:
    type: json
    data: |-
      {
        "name": "Chicken Caesar Wraps",
        "description": "Marinated chicken and lettuce wrapped in a tortilla.",
        "price": 20.00,
        "cook_time": 20,
        "servings": 8
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
