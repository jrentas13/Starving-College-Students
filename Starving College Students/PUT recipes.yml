info:
  name: PUT recipes
  type: http
  seq: 3

http:
  method: PUT
  url: http://127.0.0.1:8080/recipes/:recipe_id
  params:
    - name: recipe_id
      value: "2"
      type: path
  body:
    type: json
    data: |-
      {
        "tags": ["chicken"],
        "prep_time": 8
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
